-- Shadow Sun V2 (Protected & Obfuscated Loader)
local _0xA = "https://sirius.menu/rayfield"
local _0xB = loadstring(game:HttpGet(_0xA))()

local _0xC = _0xB:CreateWindow({
    Name = "⚡ Shadow Sun | สคริปต์ฮับพรีเมียม V2 ⚡",
    LoadingTitle = "🌐 กำลังโหลดระบบ Shadow Sun Engine...",
    LoadingSubtitle = "สร้างโดย: ซัน (Sun)",
    ConfigurationSaving = { Enabled = true, FolderName = "ShadowSunHub", FileName = "Config" },
    Discord = { Enabled = false, Invite = "", RememberJoins = true },
    KeySystem = false,
    Theme = "Bloom"
})

-- Encrypted Payload Function Execution
local function _0xD(_0xE)
    local _0xF = ""
    for _0x10 = 1, #_0xE do
        _0xF = _0xF .. string.char(string.byte(_0xE, _0x10) ~ 17)
    end
    return _0xF
end

-- Core Engine Initialization
task.spawn(function()
    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local Workspace = game:GetService("Workspace")
    local UserInputService = game:GetService("UserInputService")

    local LocalPlayer = Players.LocalPlayer
    local Mouse = LocalPlayer:GetMouse()
    local Camera = Workspace.CurrentCamera

    local PrimaryColor = Color3.fromRGB(180, 50, 255)
    local ESPColor = Color3.fromRGB(255, 0, 128)

    local SilentAimEnabled, WallbangEnabled, TeamCheck = false, false, false
    local AimPart, FOVRadius, ShowFOV, FOVTransparency = "Head", 150, false, 0.8
    local HitboxEnabled, HitboxSize, HitboxTransparency = false, 2, 0.5
    local OriginalSizes = {}

    local ESPMode, TracersEnabled, NameESPEnabled = "Off", false, false
    local ESPObjects, Tracers, NameTags = {}, {}, {}
    local SelectedPlayerName = ""
    local WalkSpeedValue, JumpPowerValue = 16, 50
    local SpeedEnabled, JumpEnabled, InfJumpEnabled, NoclipEnabled = false, false, false, false

    local FOVCircle = Drawing.new("Circle")
    FOVCircle.Color = PrimaryColor
    FOVCircle.Thickness = 1.8
    FOVCircle.NumSides = 60
    FOVCircle.Filled = false
    FOVCircle.Visible = false
    FOVCircle.Transparency = FOVTransparency

    local function IsSameTeam(p)
        return TeamCheck and LocalPlayer.Team and p.Team and LocalPlayer.Team == p.Team
    end

    local function GetClosestTarget()
        local Closest, MaxDist = nil, FOVRadius
        for _, v in pairs(Players:GetPlayers()) do
            if v ~= LocalPlayer and not IsSameTeam(v) and v.Character and v.Character:FindFirstChild(AimPart) and v.Character:FindFirstChildOfClass("Humanoid") then
                if v.Character.Humanoid.Health > 0 then
                    local Pos, OnScreen = Camera:WorldToViewportPoint(v.Character[AimPart].Position)
                    if OnScreen then
                        local Dist = (Vector2.new(Mouse.X, Mouse.Y) - Vector2.new(Pos.X, Pos.Y)).Magnitude
                        if Dist < MaxDist then
                            if not WallbangEnabled then
                                local Params = RaycastParams.new()
                                Params.FilterType = Enum.RaycastFilterType.Exclude
                                Params.FilterDescendantsInstances = {LocalPlayer.Character, Camera}
                                local Res = Workspace:Raycast(Camera.CFrame.Position, (v.Character[AimPart].Position - Camera.CFrame.Position), Params)
                                if Res and Res.Instance:IsDescendantOf(v.Character) then MaxDist = Dist; Closest = v end
                            else MaxDist = Dist; Closest = v end
                        end
                    end
                end
            end
        end
        return Closest
    end

    local function GetPlayerList()
        local l = {}
        for _, p in pairs(Players:GetPlayers()) do if p ~= LocalPlayer then table.insert(l, p.Name) end end
        if #l == 0 then table.insert(l, "ไม่พบผู้เล่นอื่น") end
        return l
    end

    local mt = getrawmetatable(game)
    local oldIndex = mt.__index
    setreadonly(mt, false)
    mt.__index = newcclosure(function(self, idx)
        if not checkcaller() and SilentAimEnabled and idx == "Hit" then
            local t = GetClosestTarget()
            if t and t.Character and t.Character:FindFirstChild(AimPart) then return t.Character[AimPart].CFrame end
        end
        return oldIndex(self, idx)
    end)
    setreadonly(mt, true)

    RunService.RenderStepped:Connect(function()
        FOVCircle.Visible = ShowFOV and SilentAimEnabled
        FOVCircle.Radius = FOVRadius
        FOVCircle.Position = Vector2.new(Mouse.X, Mouse.Y + 36)
        FOVCircle.Transparency = FOVTransparency
    end)

    RunService.RenderStepped:Connect(function()
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= LocalPlayer and not IsSameTeam(plr) and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = plr.Character.HumanoidRootPart
                if not OriginalSizes[plr] then OriginalSizes[plr] = hrp.Size end
                if HitboxEnabled and plr.Character:FindFirstChildOfClass("Humanoid") and plr.Character.Humanoid.Health > 0 then
                    hrp.Size = Vector3.new(HitboxSize, HitboxSize, HitboxSize)
                    hrp.Transparency = HitboxTransparency
                    hrp.Color = PrimaryColor
                    hrp.Material = Enum.Material.ForceField
                    hrp.CanCollide = false
                elseif OriginalSizes[plr] then
                    hrp.Size = OriginalSizes[plr]
                    hrp.Transparency = 1
                end
            end
        end
    end)

    RunService.Stepped:Connect(function()
        if NoclipEnabled and LocalPlayer.Character then
            for _, p in pairs(LocalPlayer.Character:GetDescendants()) do if p:IsA("BasePart") then p.CanCollide = false end end
        end
    end)

    UserInputService.JumpRequest:Connect(function()
        if InfJumpEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
            LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)

    RunService.RenderStepped:Connect(function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
            local h = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            if SpeedEnabled then h.WalkSpeed = WalkSpeedValue end
            if JumpEnabled then h.JumpPower = JumpPowerValue end
        end
    end)

    local function ClearESP(p)
        if ESPObjects[p] then for _, o in pairs(ESPObjects[p]) do if typeof(o) == "Instance" then o:Destroy() end end ESPObjects[p] = nil end
        if Tracers[p] then Tracers[p]:Remove() Tracers[p] = nil end
        if NameTags[p] then NameTags[p]:Remove() NameTags[p] = nil end
    end

    local function UpdateESP()
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                ClearESP(p)
                if p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChildOfClass("Humanoid") and p.Character.Humanoid.Health > 0 and not IsSameTeam(p) then
                    ESPObjects[p] = {}
                    if ESPMode == "Highlight" then
                        local hl = Instance.new("Highlight")
                        hl.Parent, hl.FillColor, hl.OutlineColor, hl.FillTransparency, hl.OutlineTransparency = p.Character, ESPColor, Color3.fromRGB(255,255,255), 0.5, 0
                        table.insert(ESPObjects[p], hl)
                    elseif ESPMode == "Box" then
                        local bx = Instance.new("SelectionBox")
                        bx.Adornee, bx.Color3, bx.LineThickness, bx.Parent = p.Character, ESPColor, 0.05, p.Character
                        table.insert(ESPObjects[p], bx)
                    end
                    if TracersEnabled then
                        local l = Drawing.new("Line") l.Color, l.Thickness = ESPColor, 1.5; Tracers[p] = l
                    end
                    if NameESPEnabled then
                        local t = Drawing.new("Text") t.Color, t.Size, t.Center, t.Outline = Color3.fromRGB(255,255,255), 16, true, true; NameTags[p] = t
                    end
                end
            end
        end
    end

    RunService.RenderStepped:Connect(function()
        for p, l in pairs(Tracers) do
            if p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChildOfClass("Humanoid") and p.Character.Humanoid.Health > 0 then
                local Pos, On = Camera:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)
                if On then l.From = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y); l.To = Vector2.new(Pos.X, Pos.Y); l.Visible = true else l.Visible = false end
            else l.Visible = false end
        end
        for p, t in pairs(NameTags) do
            if p.Character and p.Character:FindFirstChild("Head") and p.Character:FindFirstChildOfClass("Humanoid") and p.Character.Humanoid.Health > 0 then
                local Pos, On = Camera:WorldToViewportPoint(p.Character.Head.Position + Vector3.new(0, 2, 0))
                if On then t.Position = Vector2.new(Pos.X, Pos.Y); t.Text = p.Name .. " [" .. math.floor(p.Character.Humanoid.Health) .. " HP]"; t.Visible = true else t.Visible = false end
            else t.Visible = false end
        end
    end)

    Players.PlayerAdded:Connect(function(p) p.CharacterAdded:Connect(function() task.wait(1); UpdateESP() end) end)
    Players.PlayerRemoving:Connect(ClearESP)

    -- UI TAB SETUP
    local T1 = _0xC:CreateTab("🎯 ระบบต่อสู้", "crosshair")
    local T2 = _0xC:CreateTab("👁️ ระบบมอง (ESP)", "eye")
    local T3 = _0xC:CreateTab("⚡ ฟังก์ชันตัวละคร", "user")
    local T4 = _0xC:CreateTab("⚙️ ตั้งค่าระบบ", "settings")

    T1:CreateSection("🎯 ล็อคเป้าอัตโนมัติ (Silent Aim)")
    T1:CreateToggle({Name = "เปิดใช้งาน Silent Aim", CurrentValue = false, Flag = "SA", Callback = function(v) SilentAimEnabled = v end})
    T1:CreateToggle({Name = "ยิงทะลุกำแพง (Wallbang)", CurrentValue = false, Flag = "WB", Callback = function(v) WallbangEnabled = v end})
    T1:CreateToggle({Name = "เช็คทีมเดียวกัน (ไม่ยิงพวกเดียวกัน)", CurrentValue = false, Flag = "TC", Callback = function(v) TeamCheck = v; UpdateESP() end})
    T1:CreateDropdown({Name = "ส่วนของร่างกายที่จะล็อค", Options = {"Head", "HumanoidRootPart"}, CurrentOption = {"Head"}, MultipleOptions = false, Flag = "AP", Callback = function(o) AimPart = o[1] end})
    T1:CreateSection("⭕ ตั้งค่าวงกลม FOV")
    T1:CreateToggle({Name = "แสดงวงกลม FOV", CurrentValue = false, Flag = "SF", Callback = function(v) ShowFOV = v end})
    T1:CreateSlider({Name = "ขนาดรัศมี FOV", Range = {50, 500}, Increment = 10, Suffix = " px", CurrentValue = 150, Flag = "FR", Callback = function(v) FOVRadius = v end})
    T1:CreateSlider({Name = "ความจางของวงกลม FOV", Range = {0, 1}, Increment = 0.05, Suffix = "", CurrentValue = 0.8, Flag = "FT", Callback = function(v) FOVTransparency = v end})
    T1:CreateSection("📦 ขยายขนาดตัวศัตรู (Hitbox)")
    T1:CreateToggle({Name = "เปิดใช้งานขยาย Hitbox", CurrentValue = false, Flag = "HB", Callback = function(v) HitboxEnabled = v end})
    T1:CreateSlider({Name = "ขนาด Hitbox", Range = {2, 50}, Increment = 1, Suffix = " studs", CurrentValue = 2, Flag = "HS", Callback = function(v) HitboxSize = v end})
    T1:CreateSlider({Name = "ความโปร่งใสของ Hitbox", Range = {0, 1}, Increment = 0.1, Suffix = "", CurrentValue = 0.5, Flag = "HT", Callback = function(v) HitboxTransparency = v end})

    T2:CreateSection("👁️ มองทะลุ (Player ESP)")
    T2:CreateDropdown({Name = "รูปแบบมองทะลุ", Options = {"Off", "Box", "Highlight"}, CurrentOption = {"Off"}, MultipleOptions = false, Flag = "EM", Callback = function(o) ESPMode = o[1]; UpdateESP() end})
    T2:CreateColorPicker({Name = "สีของมองทะลุ", Color = Color3.fromRGB(255, 0, 128), Flag = "EC", Callback = function(v) ESPColor = v; UpdateESP() end})
    T2:CreateToggle({Name = "เส้นชี้ตำแหน่งผู้เล่น (Tracers)", CurrentValue = false, Flag = "TR", Callback = function(v) TracersEnabled = v; UpdateESP() end})
    T2:CreateToggle({Name = "แสดงชื่อและเลือด", CurrentValue = false, Flag = "NE", Callback = function(v) NameESPEnabled = v; UpdateESP() end})
    T2:CreateSection("📷 มุมมองกล้อง")
    T2:CreateSlider({Name = "ระยะมุมมองกล้อง (FOV)", Range = {70, 120}, Increment = 1, Suffix = "°", CurrentValue = 70, Flag = "CF", Callback = function(v) Camera.FieldOfView = v end})

    T3:CreateSection("🏃ปรับแต่งการเคลื่อนที่")
    T3:CreateToggle({Name = "เปิดใช้งาน เดินเร็ว", CurrentValue = false, Flag = "SE", Callback = function(v) SpeedEnabled = v end})
    T3:CreateSlider({Name = "ความเร็วการเดิน (WalkSpeed)", Range = {16, 200}, Increment = 1, Suffix = " speed", CurrentValue = 16, Flag = "WS", Callback = function(v) WalkSpeedValue = v end})
    T3:CreateToggle({Name = "เปิดใช้งาน กระโดดสูง", CurrentValue = false, Flag = "JE", Callback = function(v) JumpEnabled = v end})
    T3:CreateSlider({Name = "แรงกระโดด (Jump Power)", Range = {50, 300}, Increment = 5, Suffix = " power", CurrentValue = 50, Flag = "JP", Callback = function(v) JumpPowerValue = v end})
    T3:CreateToggle({Name = "กระโดดไม่จำกัด (Inf Jump)", CurrentValue = false, Flag = "IJ", Callback = function(v) InfJumpEnabled = v end})
    T3:CreateToggle({Name = "เดินทะลุกำแพง (Noclip)", CurrentValue = false, Flag = "NC", Callback = function(v) NoclipEnabled = v end})
    T3:CreateSection("📍 ระบบวาร์ป (Teleport)")
    local PD = T3:CreateDropdown({Name = "เลือกผู้เล่นที่จะวาร์ปไปหา", Options = GetPlayerList(), CurrentOption = {GetPlayerList()[1] or ""}, MultipleOptions = false, Flag = "TPD", Callback = function(o) SelectedPlayerName = o[1] end})
    T3:CreateButton({Name = "วาร์ปไปหาผู้เล่นที่เลือก", Callback = function()
        if SelectedPlayerName ~= "" and SelectedPlayerName ~= "ไม่พบผู้เล่นอื่น" then
            local tp = Players:FindFirstChild(SelectedPlayerName)
            if tp and tp.Character and tp.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                LocalPlayer.Character.HumanoidRootPart.CFrame = tp.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                _0xB:Notify({Title = "ระบบวาร์ป", Content = "วาร์ปสำเร็จ!", Duration = 3})
            end
        end
    end})
    T3:CreateButton({Name = "รีเฟรชรายชื่อผู้เล่น", Callback = function() PD:Refresh(GetPlayerList()) end})

    T4:CreateSection("🎨 ปรับแต่งธีมและสีเมนู")
    T4:CreateDropdown({Name = "ธีมเมนู (GUI Theme)", Options = {"Bloom", "Default", "Dark", "Ocean", "Amber", "Ruby", "Midnight", "Serenity"}, CurrentOption = {"Bloom"}, MultipleOptions = false, Flag = "TH", Callback = function(o) _0xC:ModifyTheme(o[1]) end})
    T4:CreateColorPicker({Name = "สีหลักของเมนู", Color = Color3.fromRGB(180, 50, 255), Flag = "PC", Callback = function(v) PrimaryColor = v; FOVCircle.Color = v end})
    T4:CreateKeybind({Name = "ปุ่ม เปิด/ปิด หน้าต่างเมนู", CurrentKeybind = "RightControl", HoldToInteract = false, Flag = "KB", Callback = function() end})
    T4:CreateSection("👑 ผู้พัฒนาและเครดิต")
    T4:CreateParagraph({Title = "☀️ Shadow Sun Hub V2", Content = "ผู้พัฒนา: ซัน (Sun)\nเวอร์ชัน: 2.5 (Protected Build)\nขับเคลื่อนโดย Rayfield Library"})
    T4:CreateButton({Name = "คัดลอกลิงก์ TikTok ผู้พัฒนา", Callback = function()
        setclipboard("https://www.tiktok.com/@sun_ggez0?_r=1&_t=ZS-98SSUSAnjHg")
        _0xB:Notify({Title = "คัดลอกเรียบร้อย", Content = "คัดลอกลิงก์ TikTok แล้ว!", Duration = 3})
    end})

    _0xB:LoadConfiguration()
end)
